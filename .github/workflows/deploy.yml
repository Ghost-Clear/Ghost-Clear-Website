

name: CD
on:
  push:
    branches: [ master ]

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v2

    - name: Run a multi-line script
      run: |
        ftp ftp.ghostclear.com username u338616000.varunchitturi password Var29Aug!
        ls
        delete *
        mput *
        ls
        bye
        
        
