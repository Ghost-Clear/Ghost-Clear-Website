

name: CD
on:
  push:
    branches: [ master ]

jobs:
  deploy:

    runs-on: macos-latest

    steps:

    - uses: actions/checkout@v2
    - name: Update Files
      run: |
        chmod +x deploy.sh
        ./deploy.sh



